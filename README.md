# CometChat React + Vite (Internship Task)

This is a minimal React + Vite implementation that integrates the CometChat JS SDK and demonstrates:
- App initialization
- Login/create user (using UID)
- Fetching users
- One-to-one messaging (send & receive)

## Credentials used (already filled here)
- App ID: `16723417fa198c`
- Region: `IN`
- Auth Key: `f102966166f86786b31c3ba3f1cbf6deb2dbdc86`

> For security you may want to move these into environment variables or .env in your repo.

## Install & Run

```bash
npm install
npm run dev
